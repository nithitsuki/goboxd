{
  "language": "pony",
  "source": "use \"files\"\nactor Main\n  new create(env: Env) =>\n    try\n      let path = FilePath(FileAuth(env.root), \"/etc/hosts\")\n      let f = File.create(path)\n      if f.errno() is FileOK then f.print(\"x\") end\n    end\n",
  "tests": [
    {
      "stdin": "",
      "expected_stdout": ""
    }
  ],
  "run": {
    "limits": {
      "wall_time_s": 4,
      "memory_kb": 2097152,
      "max_processes": 64
    }
  }
}